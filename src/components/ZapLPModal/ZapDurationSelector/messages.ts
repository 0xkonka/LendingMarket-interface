import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  maximum: 'Maximum multiplier',
  optionUnavailable: '*1-month lock is not available for partially vested RDNT.',
});
