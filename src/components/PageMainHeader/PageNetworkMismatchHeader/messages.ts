import { defineMessages } from 'react-intl';

export default defineMessages({
  title: 'Chain not supported',
  description: 'Please connect to a supported network',
  button: 'Connect to {networkConfig}',
});
