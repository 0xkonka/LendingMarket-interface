import { defineMessages } from 'react-intl';

export default defineMessages({
  noBorrows: 'No borrows detected',
  noData: 'No Data',
  borrowComposition: 'Borrow composition',
  borrowAvailable: 'Borrowing power available',
});
