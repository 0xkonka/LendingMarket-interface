import { defineMessages } from 'react-intl';

export default defineMessages({
  title: 'Lock duration setting',
  subTitle: 'Auto relock',
  description: 'Automatically relock your dLP for another {duration} when your lock expires',
});
