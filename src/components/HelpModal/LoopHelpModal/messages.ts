import { defineMessages } from 'react-intl';

export default defineMessages({
  caption: 'ETH required to loop',
  description:
    'If lacking sufficient dLP for emissions eligibility, the loop function converts borrowed ETH to dLP to maintain minimum requirements',
  notificationIconTooltipText: '',
});
