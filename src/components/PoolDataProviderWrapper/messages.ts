import { defineMessages } from 'react-intl';

export default defineMessages({
  errorTitle: 'Something went wrong',
  errorDescription: 'Please report the problem to our developer team so they can look into it.',
});
