import { defineMessages } from 'react-intl';

export default defineMessages({
  autoRelock: 'Auto relock',
  autoRelockDescription:
    'Automatically relock expired dLP for your default lock length of {duration}',
});
